# using the assignment operator to assign the value into the variablw on the left

# create name variable

first_name = 'Michelle'
last_name = 'Quaye'

# variables contain data
# print function calls data
# Concatenation - glues strings together
# print function send the output to standard output stream which is sent to the terminal
print(first_name + ' ' + last_name)



